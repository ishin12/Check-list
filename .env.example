# Copy to .env.local and fill in from your Supabase project (Settings → API).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: Resend API key for the `notify` Edge Function (server-side only,
# not used by the browser). Set as a Supabase function secret, not in .env.
# RESEND_API_KEY=
